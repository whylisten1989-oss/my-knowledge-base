# 客服绩效 BI 中文开发日志

> 本文件只追加、不覆盖旧记录。测试状态按实际执行结果填写；没有执行或没有验证的事项会明确注明。

## 2026-07-22 03:06（阶段基线）

### 当前分支

`feature/customer-service-bi`。本次开发继续在独立功能分支上进行，不合并到 `main`。

### 当前提交

`bc47c2a feat: add customer service BI v1`

### 本阶段完成内容

- 已建立可打开的 Customer BI V1 页面和五步 Excel 导入向导。
- 已实现 Excel 浏览器端真实解析、业务日期选择、人员勾选、KPI 校验预览和确认保存流程。
- 已实现满意率、工作时间平均响应时长、转化率、综合得分和日排名计算。
- 已生成独立 Supabase 数据库 SQL，页面只配置 Project URL 和 publishable key（公开前端密钥）。
- 已实现第一版驾驶舱、人员详情抽屉、Skeleton 骨架屏、Toast 提示、卡片抬升和分步过渡动画。
- 微信小店相关字段在解析阶段主动忽略，不参与任何 KPI、排名和展示。
- 本轮新增要求中的“昨日、近 7 个有效业务日、本月”统一时间维度尚未开始改造。
- 按用户最新要求，手机端适配暂停；不会在本轮继续扩大手机端开发范围。

### 为什么这样实现

当前网站是 Vue 3 CDN 静态多页面架构，没有 npm 构建流程。Customer BI 因此继续采用独立 HTML、CSS 和 JavaScript 文件，避免影响现有 Finance、Habits、Wiki、Profile 页面，也避免把项目改造成 Vite、Next.js 等全新工程。

Excel 先在浏览器本地解析，用户确认人员和结果后才写入数据库。这样可以防止文件里的无关人员被自动计入团队指标和排名，也可以在正式保存前发现字段问题。

### 新增文件

- `customer-bi.html`：客服绩效 BI 主页面。
- `css/customer-bi.css`：驾驶舱、导入向导、人员详情和动效样式。
- `js/customer-bi-config.js`：独立 Supabase Project URL 与 publishable key 配置。
- `js/customer-bi-core.js`：Excel 解析、KPI 评分、团队汇总和排名的纯计算逻辑。
- `js/customer-bi.js`：Vue 页面状态、导入流程、Supabase 读写和 ECharts 图表逻辑。
- `supabase/customer-bi-v1.sql`：最小 V1 数据表、约束、索引和 RLS（行级安全策略）完整 SQL。
- `docs/customer-bi-design/`：阶段一设计图、说明和 V1 使用说明。

### 修改文件

未修改现有生产业务页面和 `js/config.js`。

### 数据库变更

SQL 已包含 `bi_agents`、`bi_import_batches`、`bi_import_batch_agents`、`bi_daily_metrics`、`bi_daily_rankings`，以及基础的 `bi_achievements`、`bi_kpi_rules`、`bi_team_daily_summary`。

- 已设计唯一约束，防止同一用户同一业务日期保留多个当前批次。
- 已设计常用查询索引。
- 已启用 RLS，并限制登录用户读取和写入自己的数据。
- SQL 状态：**已生成，尚未执行**。
- Supabase 状态：**尚未验证连接**。
- 正式数据写入：**尚未验证**。

### 功能实现状态

- Excel 真实解析：已完成。
- 人员全选、取消全选、反选和搜索：已完成。
- KPI 计算与日排名：已完成。
- Supabase 保存代码：已完成；数据库尚未初始化，因此真实保存尚未验证。
- 昨日看板：部分完成；已有单日快照展示，但尚未改成“最近 confirmed 有效业务日”的正式定义。
- 近 7 个有效业务日：尚未开始。
- 本月自然月聚合：尚未开始。
- 人员详情三时间维度：尚未开始。
- 手机端追加适配：按用户要求暂停。
- Vercel Preview：**尚未生成**。

### 测试结果

- 测试输入：用户提供的 `数据参考722.xlsx`。
- 测试内容：读取第一个工作表、识别人员、识别工作时间平均响应时长、计算满意率和转化率、忽略微信小店字段。
- 预期结果：文件能够真实解析；无关字段不阻塞其他数据；只对有效且选中的人员计算。
- 实际结果：识别到 54 行人员记录，其中 9 行具备完整核心 KPI；9 人团队满意率约 81.11%，转化率约 43.31%，简单平均响应时长约 17.56 秒。
- 测试结论：Excel 解析与已确认评分阈值通过本地计算测试。
- 浏览器测试：页面、导入向导、真实 Excel 选择与 KPI 预览已经运行；控制台仅观察到 Tailwind CDN 的生产环境提示，不影响本地 V1 功能。
- Supabase 保存测试：未执行，因为 SQL 尚未在目标项目中运行。

### 已知问题

- 当前 Dashboard 的日、周、月趋势切换不符合最新业务定义，需要统一改为昨日、近 7 个有效业务日、本月。
- 当前人员详情只展示单日值和已有历史趋势，需要增加相同的三个时间维度。
- 页面依赖 CDN；离线环境下 Vue、ECharts、SheetJS、Supabase SDK 和 Tailwind 可能无法加载。
- SQL 未执行前，页面可以解析和预览 Excel，但不能保存或读取正式历史快照。

### 需要用户操作

当前阶段不要求立即操作。需要启用正式保存时，只需：

1. 打开 Supabase。
2. 点击左侧 SQL Editor。
3. 点击 New query。
4. 粘贴 `supabase/customer-bi-v1.sql` 的完整内容。
5. 点击 Run。

### 下一步计划

先实现可独立测试的有效业务日范围、近 7 日和本月聚合规则，再改造 Dashboard 和人员详情。这样可以先保证统计口径正确，再处理页面动效和展示，避免图表看起来正常但底层数据口径错误。

## 2026-07-22 03:18（有效业务日聚合规则）

### 当前分支

`feature/customer-service-bi`

### 当前提交

阶段开始时为 `4b32475 docs(bi): 建立中文开发日志`。

### 本阶段完成内容

- 新增“昨日”范围解析：以数据库最新有效业务日期为基准，并找到前一个有效业务日用于对比。
- 新增“近 7 个有效业务日”范围解析：只取真实存在数据的日期，自动跳过空白自然日；对比范围取此前连续 7 个有效业务日。
- 新增“本月”范围解析：以最新有效业务日期所在自然月为当前月，并按相同日号截取上月同期。
- 新增按人员跨日聚合：满意率汇总好评和差评后计算，转化率汇总下单人数和询单人数后计算，工作时间平均响应时长按有效参与人日简单平均。
- 新增跨周期综合得分重新计算和排名，不读取或平均每日名次。
- 新增近 7 日至少参与 2 天、本月至少参与 50% 且不少于 3 天的资格标记。
- 新增本月只有 1 至 2 个业务日时的临时排行标记，不生成正式冠军结果。

### 为什么这样实现

时间范围和聚合被放在 `customer-bi-core.js` 的纯计算函数中，不依赖 Vue 页面或 Supabase。这样同一套口径可以同时供 Dashboard、排行榜和人员详情使用，也能直接用 Node.js 做断言测试，降低三个页面口径不一致的风险。

本月基准使用“最新 confirmed 快照所在月份”，而不是直接用电脑当前日期。这样当数据库暂时停止更新或当前月还没有任何数据时，页面不会凭空显示一个无数据月份；同时标题会展示真实截止日期。

### 新增文件

无。

### 修改文件

- `js/customer-bi-core.js`：新增已保存指标标准化、有效业务日范围、跨日人员聚合、团队聚合和周期排名函数。
- `docs/customer-bi-design/DEVELOPMENT_LOG.md`：追加本阶段真实开发和测试记录。

### 数据库变更

本阶段没有修改 SQL。所有周期结果均可由已保存的每日快照实时聚合，避免为了 V1 再增加周表或月表。

- SQL 状态：已生成，尚未执行。
- Supabase 连接：尚未验证。
- Vercel Preview：尚未生成。

### 功能实现状态

- 有效业务日范围：已完成。
- 近 7 日聚合口径：已完成纯计算层。
- 本月聚合口径：已完成纯计算层。
- 参与资格与样本不足标记：已完成纯计算层。
- Dashboard 接入：尚未开始。
- 人员详情接入：尚未开始。

### 测试结果

- 测试内容：JavaScript 语法检查；昨日、近 7 日、本月日期切片；上月同期；跨日满意率、转化率、平均响应时长；最低参与门槛。
- 测试输入：构造了包含非连续业务日期、两名人员和三个人日记录的最小可核算数据。
- 预期结果：近 7 日只取最后 7 个有效日期；甲参与 2 天且可进入正式榜，乙只参与 1 天且标记样本不足；团队响应时长按 3 个人日平均。
- 实际结果：全部断言通过，终端输出 `period aggregation tests passed`。
- 测试结论：通过。
- 说明：本阶段测试的是纯计算规则，尚未声称 Supabase 历史数据查询已经验证。

### 已知问题

- 页面仍在使用旧的日、周、月趋势控件，尚未消费新的时间范围函数。
- 当前月对比只有在上月同期存在数据时才提供；没有上月数据时必须在页面显示“暂无完整对比”。

### 需要用户操作

无。

### 下一步计划

将 Dashboard 顶部改成“昨日 / 近 7 日 / 本月”统一切换，并让 KPI 卡、趋势、排行、矩阵、洞察和人员卡片同时更新。之后再以相同方式接入人员详情。

## 2026-07-22 03:42（Dashboard 三时间维度）

### 当前分支

`feature/customer-service-bi`

### 当前提交

阶段开始时为 `c45f173 feat(bi): 完成有效业务日聚合规则`。

### 本阶段完成内容

- Dashboard 顶部已改为“昨日 / 近 7 日 / 本月”三个清晰切换按钮，默认选择“昨日”。
- “昨日”读取最近一个 confirmed（已确认）且存在正式指标的业务日期，不使用电脑日期减一天。
- 六张 KPI 卡、趋势图、排行榜、达标矩阵、洞察和人员卡片现在随同一个时间按钮同步更新。
- KPI 卡增加前一对应周期变化；数据不够时显示“暂无完整对比”，不生成假数字。
- 趋势图只显示当前所选范围；昨日模式额外带上前一个有效业务日作为对照点。
- 近 7 日和本月排行榜使用聚合 KPI 重新计算综合得分和名次，不平均每日名次。
- 样本不足人员仍可查看，但在排行榜和卡片上明确标记；正式冠军从满足参与条件的人员中产生。
- 本月只有 1 至 2 个业务日时显示“月度样本积累中”和临时排行，不生成正式冠军。
- 排行榜标题包含真实日期范围、当前人员范围和参与人数。
- 保留已有 150–300ms 的克制动效，并为整块周期内容增加轻微淡入上浮切换。

### 为什么这样实现

首页没有把三个周期纵向堆成长页面，而是复用同一套卡片和图表区域。这样用户切换周期时能在相同位置比较信息，也符合“首页最多展示到月维度”的要求。

confirmed 批次与每日指标同时存在才视为有效业务日期。这样可以排除只有批次记录却没有正式指标的异常数据，也不会把导入向导尚未确认的内容展示为正式 Dashboard 数据。

### 新增文件

无。

### 修改文件

- `customer-bi.html`：重排 Dashboard 顶部时间切换、KPI 对比信息、周期化标题和样本不足标记。
- `css/customer-bi.css`：新增三段式切换、KPI 变化状态、周期切换和样本标签样式。
- `js/customer-bi.js`：查询 confirmed 批次，接入周期范围、跨日聚合、对比、排行、趋势和洞察逻辑。
- `docs/customer-bi-design/DEVELOPMENT_LOG.md`：追加本阶段记录。

### 数据库变更

没有新增表或字段。Dashboard 查询现有 `bi_import_batches` 的 confirmed 状态，并实时聚合 `bi_daily_metrics`。

- SQL 状态：已生成，尚未执行。
- Supabase 连接：尚未验证。
- 数据真实写入：尚未验证。
- Vercel Preview：尚未生成。

### 功能实现状态

- 昨日 KPI、正式日排行和前一有效业务日对比：已接入页面逻辑。
- 近 7 日汇总、重新评分、重新排名和此前 7 个有效业务日对比：已接入页面逻辑。
- 本月累计、月排行、参与门槛和上月同期对比：已接入页面逻辑。
- Dashboard 三维度同步：已完成代码实现。
- 人员详情三维度：尚未完成。
- Supabase 真实历史数据页面验证：被数据库尚未初始化阻塞。

### 测试结果

- JavaScript 语法检查：通过。
- Git 空白和冲突标记检查：通过。
- 禁用维度检查：Dashboard 页面与脚本中未找到“本周、上周、季度、年度”入口，也未保留旧 `dashboardDate` 和 `trendPeriod` 状态。
- 聚合数值正确性：沿用上一阶段已通过的纯计算断言。
- 浏览器真实历史数据切换：尚未执行。原因是 Supabase SQL 尚未执行，当前没有可读取的 confirmed 历史数据；没有使用 mock 数据伪造通过结果。

### 已知问题

- 在 Supabase 未初始化的环境中，登录后的历史查询会提示先运行 SQL，这是预期保护，不代表已完成云端验证。
- 当前“人员范围”显示为每个历史 confirmed 批次实际保存的人员集合；多名单功能不属于本次 V1 范围。
- 人员详情仍使用旧的单一视图，需要下一阶段改造。

### 需要用户操作

无。云端验证会在用户之后运行 SQL 后进行。

### 下一步计划

把人员详情抽屉接入同一套三个时间范围，增加个人聚合 KPI、周期排名、前期对比和当前周期每日趋势，并保证未参与日期不会按 0 分处理。

## 2026-07-22 04:02（人员详情三时间维度）

### 当前分支

`feature/customer-service-bi`

### 当前提交

阶段开始时为 `61769aa feat(bi): 完成昨日近7日本月看板`。

### 本阶段完成内容

- 人员详情新增“昨日 / 近 7 日 / 本月”三个时间按钮，不提供季度或年度入口。
- 人员详情的满意率、工作时间均响、转化率、综合得分、排名、趋势和参与记录随周期同步更新。
- “昨日”对比改为该人员上一次实际参与的有效业务日；中间休息、未排班或未纳入统计的日期会被跳过。
- 近 7 日和本月个人指标按参与人日重新聚合，并在聚合后重新评分和排名。
- 每张个人 KPI 卡显示相对前一对应周期的变化；数据不完整时显示“暂无完整对比”。
- 样本不足和“月度样本积累中”在详情排名位置明确提示。
- 当前周期参与记录只显示真实存在的数据，不补 0，也不把缺席解释成未达标。

### 为什么这样实现

人员详情与 Dashboard 共用相同日期范围和聚合函数，避免首页与个人页出现不同结果。只有“昨日”的个人对比做了业务化调整：团队昨日对比前一个有效业务日，个人昨日则对比该人上一个实际参与日，更符合客服排班和休息场景。

### 新增文件

无。

### 修改文件

- `customer-bi.html`：增加人员详情三时间按钮、个人周期摘要、KPI 对比和空状态。
- `css/customer-bi.css`：增加详情时间按钮、对比状态和无参与数据样式。
- `js/customer-bi.js`：增加详情周期范围、个人聚合、资格、排名、对比和趋势数据。
- `docs/customer-bi-design/DEVELOPMENT_LOG.md`：追加本阶段记录。

### 数据库变更

无。人员详情直接读取已保存每日指标，不创建个人周表、月表。

- SQL 状态：已生成，尚未执行。
- Supabase 连接：尚未验证。
- 数据真实写入：尚未验证。
- Vercel Preview：尚未生成。

### 功能实现状态

- 人员昨日详情：已完成代码实现。
- 人员近 7 日聚合详情：已完成代码实现。
- 人员本月聚合详情：已完成代码实现。
- 上一有效参与日对比：已完成代码实现。
- 无参与日期不补 0：已完成。
- 浏览器真实历史快照验证：尚未完成，等待后续回归阶段。

### 测试结果

- JavaScript 语法检查：通过。
- Git 空白检查：通过。
- 禁用维度检查：人员详情 HTML 与 JavaScript 中未出现本周、上周、季度、年度入口。
- 历史缺席逻辑检查：代码只从该人员实际存在的 `business_date` 中选择上次参与日，没有生成缺席行。
- Supabase 历史数据测试：尚未执行，不伪造结果。

### 已知问题

- 未运行数据库 SQL 前无法在真实历史快照上点击人员卡片验证三周期数据。
- V1 周期结果为浏览器实时聚合；数据量较小时适合当前静态站点，未来数据量明显增长后可改为数据库视图或 RPC，但本轮不扩大架构。

### 需要用户操作

无。

### 下一步计划

启动本地静态服务器，使用浏览器检查页面加载、三个时间按钮、导入向导和控制台错误；再次使用用户真实 Excel 做解析与 KPI 回归。发现问题后直接修复，再生成最终中文状态报告。

## 2026-07-22 04:20（真实 Excel 与连接回归）

### 当前分支

`feature/customer-service-bi`

### 当前提交

阶段开始时为 `6a141d5 feat(bi): 完成人员详情时间维度`。

### 本阶段完成内容

- 使用用户提供的真实 `数据参考722.xlsx` 再次读取完整工作表并执行客服 KPI 解析。
- 检查全部 Customer BI JavaScript 文件语法。
- 检查 `customer-bi.html` 标签嵌套、唯一 `#app` 根节点和三个本地脚本引用。
- 启动本地静态服务器并确认 `customer-bi.html` 返回 HTTP 200。
- 对目标 Supabase 项目进行只读连通性检查：Auth 健康端点可访问；Customer BI 数据表端点不存在。
- 尝试使用应用内浏览器打开本地地址，但被浏览器安全策略拦截，因此没有伪造“浏览器交互通过”的结论。

### 为什么这样实现

真实 Excel 回归继续使用用户文件，而不是复制一份看起来正确的假数据。数据库检查只使用前端公开 publishable key，且只做只读请求，没有使用 secret 或 service_role。

浏览器工具明确禁止访问本地地址后，本阶段停止尝试其他绕过方式，改用静态服务器响应、语法检查和 HTML 结构检查作为安全替代。这些检查能发现文件缺失、脚本语法和标签结构问题，但不能等同于完整的点击交互测试，因此日志中明确区分。

### 新增文件

无。

### 修改文件

- `docs/customer-bi-design/DEVELOPMENT_LOG.md`：追加真实回归结果。

### 数据库变更

没有执行任何建表或写入操作。

- Supabase Auth 健康端点：HTTP 200，说明项目 Auth 服务和 URL 可达。
- `bi_agents` 只读表请求：HTTP 404，说明 Customer BI SQL 尚未在该项目中完成初始化。
- SQL 状态：**已生成，尚未执行**。
- Supabase 完整业务连接：**部分验证**。项目可达，但业务表不存在，页面历史查询和保存无法成功。
- 数据真实写入：**尚未验证，实际没有写入**。
- Vercel Preview：**尚未生成**。

### 功能实现状态

- 真实 Excel 解析：已完成并再次通过。
- 核心 KPI 计算：已完成并再次通过。
- 页面文件与 JavaScript 静态检查：已完成。
- 应用内浏览器点击回归：被本地地址安全策略阻塞。
- Supabase 项目可达性：已确认。
- Supabase 表结构：尚未创建。
- Supabase 入库：无法执行，等待 SQL 初始化。

### 测试结果

#### 真实 Excel

- 输入：`C:\Users\Administrator\Desktop\数据参考722.xlsx`。
- 工作簿：1 个工作表，`Sheet1`，使用范围 `A1:AI57`，共 35 列。
- 已确认字段：客服账号、客服昵称、有效好评数、有效差评数、工作时间平响时长、询单人数、下单人数等。
- 解析结果：54 名人员记录；其中 9 人具备完整有效核心 KPI。
- 9 人团队结果：满意率 81.11%，转化率 43.31%，工作时间响应时长简单平均 17.56 秒，人员综合得分平均 59.44。
- 综合前三名：等等 110 分、售后楠楠 110 分、售后小舒 105 分。
- 预期结果与实际结果一致：通过。

#### 静态页面

- 本地服务器：`customer-bi.html` 返回 HTTP 200。
- JavaScript：`customer-bi-core.js`、`customer-bi-config.js`、`customer-bi.js` 语法检查通过。
- HTML：标签结构和本地脚本引用检查通过。
- Git diff 空白检查：通过。

#### 浏览器

- 预期：打开本地 Dashboard 并点击三种周期。
- 实际：应用内浏览器返回 `ERR_BLOCKED_BY_CLIENT`，随后安全策略明确禁止继续尝试本地 URL 或使用替代浏览器绕过。
- 结论：**未通过浏览器交互验证，原因是工具策略阻塞，不是页面报错**。

### 已知问题

- Supabase 尚无 Customer BI 表，登录后查询和确认保存会失败并提示运行 SQL。
- 本轮修改后的三时间按钮尚未完成真实浏览器点击回归；目前只有 V1 旧版曾完成浏览器运行检查。
- 页面继续依赖公共 CDN，正式上线前应通过 Vercel Preview 再验证一次网络加载和交互。

### 需要用户操作

要启用 Supabase 保存，请在方便时执行：

1. 打开 Supabase。
2. 点击左侧 SQL Editor。
3. 点击 New query。
4. 粘贴 `supabase/customer-bi-v1.sql` 的完整内容。
5. 点击 Run。

### 下一步计划

根据真实状态生成 `FINAL_STATUS.md`，列清可用、部分完成、未完成、全部提交和唯一需要用户执行的数据库步骤。不会把未执行 SQL、未写入数据或未生成 Vercel Preview 写成已完成。

## 2026-07-22 04:34（最终状态报告）

### 当前分支

`feature/customer-service-bi`

### 当前提交

阶段开始时为 `0ca6cfc docs(bi): 记录真实回归测试`。

### 本阶段完成内容

- 创建 `docs/customer-bi-design/FINAL_STATUS.md`。
- 用非技术人员也能理解的中文区分：本地可用、部分完成、尚未完成和被数据库初始化阻塞的事项。
- 列出昨日、近 7 日、本月的具体实现程度。
- 列出真实 Excel、人员选择、KPI、排名、Supabase、数据写入和 Vercel Preview 的真实状态。
- 列出功能分支全部实现提交、全部新增文件、已知问题、风险和下一次建议顺序。

### 为什么这样实现

最终报告不使用一个笼统的“已完成”覆盖所有状态。页面代码完成和线上可用是两件不同的事：当前本地导入与计算已经可用，但 SQL 未运行就不能正式保存历史，因此报告分别给出本地代码完成度和正式线上可用完成度。

### 新增文件

- `docs/customer-bi-design/FINAL_STATUS.md`：最终中文状态与交接说明。

### 修改文件

- `docs/customer-bi-design/DEVELOPMENT_LOG.md`：追加本阶段记录，没有覆盖旧日志。
- `docs/customer-bi-design/V1-README.md`：把旧“日/周/月”文案更正为“昨日/近 7 个有效业务日/本月”，并明确 SQL 尚未执行。

### 数据库变更

无。

- SQL：已生成，尚未执行。
- Supabase：Auth 可达，业务表未创建。
- 数据写入：没有执行。
- Vercel Preview：尚未生成。

### 功能实现状态

- V1 本地开发：稳定阶段完成。
- 中文日志：完成。
- 最终中文状态报告：完成。
- 云端数据库初始化：等待用户执行 SQL。
- 线上端到端验证：等待数据库和 Preview。

### 测试结果

- 最终报告中的数据全部来自实际 Git 日志、文件差异、真实 Excel 回归和只读 Supabase 检查。
- 没有新增功能代码，因此本阶段不声称新增测试通过。

### 已知问题

详见 `FINAL_STATUS.md` 第 19 和第 20 节。

### 需要用户操作

操作 1：
打开 Supabase → 点击 SQL Editor → 点击 New query。

操作 2：
粘贴 `supabase/customer-bi-v1.sql` 的完整内容。

操作 3：
点击 Run。

### 下一步计划

SQL 执行后，从第一个测试日期的真实保存开始验证，再逐步验证两日对比、近 7 日和跨月数据。不要在真实入库尚未稳定前继续增加高级能力。
